-- ═══════════════════════════════════════════════════════════════════════════
-- 006 · 개발용 시드 데이터 (로컬 테스트 전용 — 프로덕션 적용 금지)
-- ═══════════════════════════════════════════════════════════════════════════

-- 주의: 이 파일은 supabase db seed 또는 로컬 개발 환경에서만 실행.
-- 프로덕션 마이그레이션에는 포함하지 않음.

-- 테스트 유저 2명 (auth.users 직접 삽입은 불가 — Supabase 대시보드에서 생성)
-- 아래는 profiles만 직접 삽입 (auth.users UUID와 반드시 일치해야 함)

-- 예시 UUID는 실제 Supabase Auth 대시보드에서 생성한 값으로 교체
-- INSERT INTO public.profiles (id, nickname, avatar_url) VALUES
--   ('00000000-0000-0000-0000-000000000001', '테스트유저1', NULL),
--   ('00000000-0000-0000-0000-000000000002', '테스트유저2', NULL);

-- 금칙어 추가 샘플 (운영 중 업데이트)
INSERT INTO public.blocked_words (word) VALUES
  ('시발'), ('씨발'), ('ㅅㅂ'), ('병신'), ('ㅂㅅ'),
  ('개새끼'), ('ㄱㅅㄲ'), ('지랄'), ('ㅈㄹ'), ('미친'),
  ('fuck'), ('shit'), ('asshole'), ('bastard'), ('bitch')
ON CONFLICT DO NOTHING;
